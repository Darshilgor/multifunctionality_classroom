import 'package:flutter/material.dart';

class DaaSyllabus extends StatelessWidget {
  const DaaSyllabus({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}

